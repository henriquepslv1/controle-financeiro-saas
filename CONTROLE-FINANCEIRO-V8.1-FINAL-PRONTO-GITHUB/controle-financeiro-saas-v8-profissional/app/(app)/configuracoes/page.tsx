import {redirect} from 'next/navigation'
import {createClient} from '@/lib/supabase/server'
export default async function Configuracoes(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const {data:p}=await s.from('profiles').select('full_name,email,role').eq('id',user.id).single();return <div className="container"><div className="page-title"><div><h1>Configurações</h1><p className="muted">Preferências e dados da sua conta.</p></div></div><div className="grid grid-2"><div className="card" style={{padding:20}}><h2 style={{marginTop:0}}>Conta</h2><p><strong>Nome:</strong> {p?.full_name||'—'}</p><p><strong>E-mail:</strong> {p?.email}</p><p><strong>Perfil:</strong> {p?.role==='MASTER_ADMIN'?'Administrador Master':'Cliente'}</p></div><div className="card" style={{padding:20}}><h2 style={{marginTop:0}}>Regionalização</h2><p>🇧🇷 <strong>Português (Brasil)</strong></p><p><strong>Moeda:</strong> Real brasileiro (R$)</p><p><strong>Data:</strong> DD/MM/AAAA</p><p><strong>Hora:</strong> 24 horas</p></div></div></div>}
