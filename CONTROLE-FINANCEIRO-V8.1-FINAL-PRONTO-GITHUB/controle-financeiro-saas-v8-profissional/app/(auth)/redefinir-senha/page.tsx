'use client'
import {useState} from 'react'
import {createClient} from '@/lib/supabase/client'
export default function RedefinirSenha(){const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [msg,setMsg]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');if(password.length<8||password!==confirm){setError('A senha precisa ter pelo menos 8 caracteres e as duas senhas devem ser iguais.');setLoading(false);return}const {error}=await createClient().auth.updateUser({password});if(error)setError(error.message);else setMsg('Senha alterada com sucesso. Você já pode entrar novamente.');setLoading(false)}return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:20}}><form onSubmit={submit} className="card" style={{width:'100%',maxWidth:430,padding:28}}><div style={{fontSize:24,fontWeight:900}}>Controle<span style={{opacity:.45}}>.Financeiro</span></div><h1>Nova senha</h1><p className="muted">Escolha uma senha forte.</p><label className="label">Nova senha<input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><div style={{height:12}}/><label className="label">Confirmar senha<input className="input" type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} required/></label>{msg&&<p className="green">{msg}</p>}{error&&<p className="red">{error}</p>}<button className="btn btn-primary" style={{width:'100%',marginTop:12}} disabled={loading}>{loading?'Salvando...':'Alterar senha'}</button></form></main>}
