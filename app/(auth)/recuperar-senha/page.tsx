'use client'
import {useState} from 'react'
import Link from 'next/link'
import {createClient} from '@/lib/supabase/client'
export default function RecuperarSenha(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setMsg('');setError('');const origin=window.location.origin;const {error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${origin}/redefinir-senha`});if(error)setError('Não foi possível enviar o e-mail.');else setMsg('Se o e-mail estiver cadastrado, você receberá um link para redefinir a senha.');setLoading(false)}return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:20}}><form onSubmit={submit} className="card" style={{width:'100%',maxWidth:430,padding:28}}><div style={{fontSize:24,fontWeight:900}}>Controle<span style={{opacity:.45}}>.Financeiro</span></div><h1>Recuperar senha</h1><p className="muted">Informe seu e-mail para receber o link.</p><label className="label">E-mail<input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label>{msg&&<p className="green">{msg}</p>}{error&&<p className="red">{error}</p>}<button className="btn btn-primary" style={{width:'100%',marginTop:12}} disabled={loading}>{loading?'Enviando...':'Enviar link'}</button><p style={{textAlign:'center'}}><Link className="link" href="/login">Voltar ao login</Link></p></form></main>}
